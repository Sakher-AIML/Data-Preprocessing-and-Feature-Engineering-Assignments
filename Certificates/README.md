Please add the original certificate image file named `infosys_certificate.png` into this folder.

Steps:
1. Save your certificate image as `infosys_certificate.png`.
2. Place it in the `Certificates/` folder in the repository root.
3. Commit and push, or share here and I will add it for you.

Note: The top-level `README.md` references `Certificates/infosys_certificate.png`.
